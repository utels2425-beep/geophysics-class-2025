name: pygmt

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.9
  - numpy
  - pandas
  - xarray
  - netcdf4
  - packaging
  - gmt
  - pygmt
  - obspy
  - jupyter
  - pip
  - git-lfs  # <--- 在這裡新增這一行

  - pip:
    - streamlit
    - matplotlib